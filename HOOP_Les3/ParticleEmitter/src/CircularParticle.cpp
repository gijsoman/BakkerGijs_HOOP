#include "CircularParticle.h"
#include "ofMain.h"


CircularParticle::CircularParticle()
{

}
void CircularParticle::sayHello()
{
	/*cout << "hoi" << endl;*/
}

void CircularParticle::update()
{
	//if (angle < PI * 2)
	//{
	//	position.x = ofGetWindowWidth() / 2 + sin(angle) * random;
	//	position.y = ofGetWindowHeight() / 2 + cos(angle) * random;
	//	angle = angle + ofRandom(0.01, 0.05);
	//}
	//else
	//{
	//	angle = 0;
	//}

}
